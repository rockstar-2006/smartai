# Server
PORT=3001
NODE_ENV=development

# Database (change to your production DB when deployed)
MONGODB_URI=mongodb://localhost:27017/quiz-app

# JWT
JWT_SECRET=replace_with_long_random_secret
JWT_EXPIRE=30d

# Frontend / Allowed origins (used by server CORS config)
CLIENT_URL=http://localhost:8080
VERCEL_URL=https://smartai-ten.vercel.app

# Email (Gmail app password - 16 chars, no spaces)
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_generated_gmail_app_password

# Gemini (server-side key) - NEVER expose this to the client
VITE_GEMINI_API_KEY=AIzaSyBFABBifL3Yz7x2pv5mP_3bu8Fk0i3x_Uc
# If you need a frontend key for any public client-only features (rare), use VITE_ prefix.
# WARNING: Anything starting with VITE_ will be embedded in the shipped frontend bundle and visible to everyone.
# Only include non-secret public tokens there.
# VITE_PUBLIC_SOME_KEY=optional_public_value

# API base used in front-end builds (set this in Vercel)
# Example: https://your-backend.example.com/api
# For local dev, you can set VITE_API_URL=http://localhost:3001/api
VITE_API_URL=http://localhost:3001/api
